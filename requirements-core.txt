# MHARS — Core Dependencies (range-pinned for cross-platform compatibility)
# Install with: pip install -r requirements-core.txt

# Web framework
fastapi>=0.100.0
uvicorn>=0.20.0
starlette>=0.27.0
websockets>=11.0
pydantic>=2.0

# ML / Deep Learning (CPU-compatible)
numpy>=1.24.0
pandas>=2.0.0
# Model pickles (IsolationForest) are trained with 1.8.x. Keep within 1.8 to
# avoid InconsistentVersionWarning / invalid results; retrain if bumping major.
scikit-learn>=1.8.0,<1.9.0
scipy>=1.10.0

# Reinforcement Learning
stable-baselines3>=2.0.0
gymnasium>=0.29.0

# PyTorch — CPU version (see requirements-gpu.txt for CUDA)
torch>=2.0.0
torchvision>=0.15.0

# Visualization
matplotlib>=3.7.0
pillow>=9.5.0

# Audio processing (for MFCC pipeline)
librosa>=0.10.0
soundfile>=0.12.0

# LLM (optional — falls back to templates if not installed)
# llama-cpp-python>=0.2.0

# System monitoring
psutil>=5.9.0
filelock>=3.0.0

# Testing
pytest>=7.0.0

# Auth & Security
passlib>=1.7.4
bcrypt<4.0.0
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.6  # required by FastAPI OAuth2PasswordRequestForm (login)

# R1 (optional): zero-shot foundation forecaster backend (MHARS_FORECASTER=foundation)
# chronos-forecasting
